<Page x:Class="EasySaveWPF.ViewNS.Create"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:view="clr-namespace:EasySaveWPF.ViewNS"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Create">

    <Grid MaxHeight="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Choisisez des sauvgardes" FontWeight="Bold" FontSize="10" HorizontalAlignment="Center" Margin="0,30"/>

        <!-- Selection chemain source -->
        <StackPanel Orientation="Vertical" Grid.Row="1" HorizontalAlignment="Center">
            <WrapPanel HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0,0,0,10">
                <Button Name="btnOpenFile" Command="{Binding btnOpenFilesSrc}">Open files</Button>
            </WrapPanel>
            <TextBox Text="{Binding OpenFileSrc, Mode=TwoWay}" BorderThickness="0"  />
        </StackPanel>

        <!-- Selection chemain destination -->
        <StackPanel Orientation="Vertical" Grid.Row="2" HorizontalAlignment="Center">
            <WrapPanel HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0,0,0,10">
                <Button Name="btnOpenFile2" Command="{Binding btnOpenFilesDest}">Open files</Button>
            </WrapPanel>
            <TextBox Text="{Binding OpenFileDest, Mode=TwoWay}" BorderThickness="0"  />
        </StackPanel>

        <!-- Boutons en bas -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10">
            <Button Content="Ajouter" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>
